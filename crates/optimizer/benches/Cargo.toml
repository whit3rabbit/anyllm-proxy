[package]
name = "anyllm_optimize_benches"
description = "FFEC criterion benchmarks over the fixture corpus"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[[bench]]
name = "optimize"
harness = false

[dependencies]
anyllm_optimize_core = { path = "../crates/optimize-core", version = "0.14.1" }
anyllm_optimize_passes = { path = "../crates/optimize-passes", version = "0.14.1" }

[dev-dependencies]
criterion = "0.5"
